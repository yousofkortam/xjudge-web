/**
 * Production settings. Same-origin by default: deploy the built frontend behind
 * the same host as the API, or set an absolute URL here for a split deployment.
 */
export const environment = {
  production: true,
  apiUrl: '',
};
